# Option 1: Install an older sqlite3 version compatible with RHEL 7
cd /root/Core-Repository/backend
npm uninstall sqlite3
npm install sqlite3@5.0.12  # Older version more compatible with RHEL 7

# Test
npm start