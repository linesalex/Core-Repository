# Version 3.3.3 - Release Notes

**Release Date:** November 16, 2024  
**Status:** ✅ Ready for Production

⚠️ **CRITICAL - Production Deployment Issues:**
1. **Memory crashes if running `npm start`** (development server) - See [PRODUCTION_DEPLOYMENT_FIX.md](PRODUCTION_DEPLOYMENT_FIX.md)
2. **You MUST use production builds** (`npm run build` + `serve`) on servers
3. **Clean caches before deploying** to save 2-4GB:
```bash
./cleanup-deployment.sh   # Linux
.\cleanup-deployment.ps1  # Windows
```
See [DEPLOYMENT_SIZE_REDUCTION.md](DEPLOYMENT_SIZE_REDUCTION.md) for details.

---

## 🎉 What's New in v3.3.3

### **KMZ Export for Network Design** 🗺️ - NEW MAJOR FEATURE

Export network designs from the Design & Pricing Tool as KMZ files for visualization in Google Earth or any KML viewer. Routes are automatically combined, color-coded, and bundled with disclaimers.

---

## 🚀 Key Features

### **Export Capabilities**
- ✅ **Primary Route Only** - Export just the primary path (displayed in red)
- ✅ **Secondary Route Only** - Export just the secondary/protection path (displayed in blue)  
- ✅ **Both Routes Combined** - Export protection service with dual-color coding in one file

### **Automated Processing**
- ✅ **Route Merging** - Individual circuit KMZ files automatically combined into single end-to-end path
- ✅ **Color Coding** - Red for primary paths, Blue for secondary paths (KML ABGR format)
- ✅ **Location Markers** - Source and destination points automatically added from locations template
- ✅ **Disclaimer Bundling** - Legal disclaimer automatically included with every export
- ✅ **Smart Naming** - Files named as `{Quote}_{Customer}_{Source}_{Destination}.kmz`

### **Template Management** (Admin Only)
- ✅ Upload and manage **locations.kmz** template (contains all POP code location markers)
- ✅ Upload and manage **disclaimer.kmz** template (mandatory for exports)
- ✅ View template status, uploader name, and upload date
- ✅ Download/replace existing templates
- ✅ Visual indicators for template status

### **Error Handling**
- ✅ Gracefully skips missing circuit KMZ files
- ✅ Reports which circuits were omitted
- ✅ Clear error messages for missing templates
- ✅ Detailed console logging for debugging

---

## 📋 How to Use

### **Admin Setup (One-Time)**
1. Navigate to **Admin → System Settings**
2. Scroll to **"KMZ Template Management"** section
3. Upload **disclaimer.kmz** (🔴 Required)
4. Upload **locations.kmz** (Optional - adds source/destination markers)

### **All Users**
1. Complete a network design in Design & Pricing Tool (primary and/or secondary routes)
2. Click **"Export KMZ"** button (located next to "Export Results")
3. Select route type:
   - Primary Route Only
   - Secondary Route Only
   - Both Routes (Protection Service)
4. Click **"Export KMZ"**
5. File downloads automatically
6. Open in Google Earth or any KML viewer

---

## 🔧 Technical Implementation

### **New Backend Components**

**1. KMZ Generation Service** (`backend/kmzGenerator.js`)
- Parses KML/KMZ files using `jszip` and `fast-xml-parser`
- Extracts LineString coordinates from individual route files
- Merges multiple route segments into single combined path
- Applies color styling (Red: `ff0000ff`, Blue: `ffff0000` in KML ABGR format)
- Extracts source/destination points from locations template by name matching
- Bundles disclaimer KMZ into subfolder structure (`disclaimer/`)
- Handles missing files gracefully with skip reporting

**2. Storage Directories**
- `backend/templates/` - Stores uploaded locations.kmz and disclaimer.kmz
- `backend/temp/` - Temporary export files (auto-cleanup after download)

**3. API Endpoints**
- `POST /kmz_templates/upload` - Upload templates (Admin only)
- `GET /kmz_templates/info/:templateType` - Get template metadata
- `GET /kmz_templates/download/:templateType` - Download current template
- `POST /network_design/export_kmz` - Generate and download KMZ from design

**4. Database Migration**
- `backend/migrations/014_add_kmz_templates.js` - Creates `kmz_templates` table
- Smart migration detects and fixes incorrect schema automatically
- Table structure:
  ```sql
  CREATE TABLE kmz_templates (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    template_type TEXT NOT NULL UNIQUE,
    filename TEXT NOT NULL,
    uploaded_by INTEGER NOT NULL,
    uploaded_at TEXT NOT NULL,
    FOREIGN KEY (uploaded_by) REFERENCES users(id)
  )
  ```

### **New Frontend Components**

**1. System Settings Enhancement** (`frontend/src/SystemSettingsManager.js`)
- New "KMZ Template Management" section
- **Locations Template Card**:
  - Upload/replace functionality
  - Shows filename, uploader, date
  - Download current template
  - Warning when missing
- **Disclaimer Template Card**:
  - Upload/replace functionality
  - Shows filename, uploader, date
  - Download current template
  - Error when missing (required for exports)
  - "Required" chip indicator

**2. Network Design Tool Enhancement** (`frontend/src/NetworkDesignTool.js`)
- New **"Export KMZ"** button with Map icon
- Export dialog with route selection:
  - Radio button options for Primary/Secondary/Both
  - Descriptive text for each option
  - Preview of filename format
  - Disabled states when routes unavailable
- Loading states during export
- Success notifications
- Enhanced error handling for blob responses

**3. API Integration** (`frontend/src/api.js`)
- `uploadKMZTemplate(templateType, file)` - Upload template files
- `getKMZTemplateInfo(templateType)` - Fetch template metadata
- `downloadKMZTemplate(templateType)` - Download templates
- `exportNetworkDesignKMZ(data)` - Export network design with blob response handling

---

## 📦 Dependencies Added

- **`jszip`** (^3.10.1) - ZIP file creation for KMZ packaging
- **`fast-xml-parser`** (^5.3.1) - KML parsing and XML manipulation

---

## 📊 Files Modified/Created

### Backend
- ✨ **NEW:** `backend/kmzGenerator.js` (490 lines)
- ✨ **NEW:** `backend/templates/` (directory)
- ✨ **NEW:** `backend/temp/` (directory)
- ✨ **NEW:** `backend/migrations/014_add_kmz_templates.js` (77 lines)
- 📝 **UPDATED:** `backend/routes.js` (+220 lines)
- 📝 **UPDATED:** `backend/package.json` (added dependencies)
- 📝 **UPDATED:** `backend/.gitignore` (ignore templates & temp KMZ files)

### Frontend
- 📝 **UPDATED:** `frontend/src/api.js` (+40 lines)
- 📝 **UPDATED:** `frontend/src/SystemSettingsManager.js` (+140 lines)
- 📝 **UPDATED:** `frontend/src/NetworkDesignTool.js` (+180 lines)

### Documentation
- ✨ **NEW:** `V3.3.3_RELEASE_NOTES.md` (this file)

### **Total Code Impact**
- ~1,150 lines of code added
- 8 files modified/created
- 1 database table added
- 4 new API endpoints
- 2 new npm packages

---

## 🔒 Security & Audit

### **Access Control**
- Template upload/download: **Admin only**
- Template viewing: **Admin only**
- KMZ export: **All users with network_design module access**

### **Audit Logging**
All KMZ exports logged to `audit_logs` table with:
- Action type: `KMZ_EXPORT`
- User ID and username
- Source and destination locations
- Export type (primary/secondary/both)
- Circuits included/skipped
- Quote request ID and customer name
- Execution time
- IP address and user agent

### **File Handling**
- Template uploads: Validated for `.kmz` extension
- File size: Handled by Multer middleware
- Temporary files: Auto-deleted after successful download
- Error cases: Cleanup on failure

---

## 🎯 User Impact

### **Sales Teams**
- ✅ Professional KMZ files for customer presentations
- ✅ Color-coded paths for clear primary/protection identification
- ✅ No manual KMZ manipulation required
- ✅ Quick export directly from design tool

### **Operations Teams**
- ✅ Visual route verification in Google Earth
- ✅ Geographic context with location markers
- ✅ Easy sharing with stakeholders

### **Administrators**
- ✅ Centralized template management
- ✅ Control over disclaimer content
- ✅ Audit trail of all exports
- ✅ Simple template updates

---

## ⚠️ Important Notes

### **Prerequisites**
1. **Disclaimer template is REQUIRED** - Exports will fail until uploaded
2. **Locations template is OPTIONAL** - Exports work without it but won't include location markers
3. **Individual route KMZ files must exist** - Missing files will be skipped and reported

### **Migration Behavior**
- Migration runs automatically on backend startup
- Detects and corrects incorrect table schema if previously created
- Safe to run multiple times (idempotent)

### **File Format**
- Output: Standard KMZ (zipped KML) format
- Compatible with: Google Earth, Google Earth Pro, QGIS, ArcGIS, and most GIS software
- Structure: 
  ```
  output.kmz
  ├── doc.kml (main route file)
  └── disclaimer/
      ├── doc.kml (disclaimer content)
      └── [any disclaimer assets]
  ```

---

## 🧪 Testing Checklist

### **Admin Testing**
- [ ] Upload disclaimer.kmz successfully
- [ ] Upload locations.kmz successfully
- [ ] View template info (filename, date, uploader)
- [ ] Download templates
- [ ] Replace existing templates

### **User Testing**
- [ ] Create network design with primary route
- [ ] Export primary route only as KMZ
- [ ] Open KMZ in Google Earth - verify red line
- [ ] Create network design with secondary route
- [ ] Export secondary route only as KMZ
- [ ] Open KMZ in Google Earth - verify blue line
- [ ] Create network design with both routes
- [ ] Export both routes as KMZ
- [ ] Open KMZ in Google Earth - verify red and blue lines
- [ ] Verify source/destination markers appear (if locations template uploaded)
- [ ] Verify disclaimer appears in KMZ

### **Error Handling Testing**
- [ ] Try export without disclaimer uploaded - see error message
- [ ] Try export with missing circuit KMZ files - see skip report
- [ ] Verify error messages are clear and actionable

---

## 🐛 Bug Fixes & Enhancements

### **KMZ Export - Circuit Lookup and Styling** 🔧

**Issues Fixed:**
1. ✅ Circuit KMZ files now looked up from database (`kmz_file_path` column) instead of assuming filename = circuit_id
2. ✅ KMZ structure now uses separate folders: "Primary Path" and "Secondary Path"
3. ✅ Line width fixed to `3.0` (was `3`) for proper rendering
4. ✅ Document name now properly formatted as `"{Quote} - {Customer} - {Source} - {Destination}"` (was "Network Route")
5. ✅ Enhanced debugging shows which KMZ files are loaded/skipped
6. ✅ Database connection passed to generator for circuit lookups

**Technical Changes:**
- `backend/kmzGenerator.js`:
  - Added `getKMZPath()` helper to query database for actual file paths
  - Modified `buildKMZ()` to create separate folders for Primary/Secondary paths
  - Updated LineStyle width from `3` to `3.0`
  - Added `documentName` parameter for proper KML document naming
- `backend/routes.js`:
  - Pass `db` connection to `generateNetworkDesignKMZ()`
  - Added detailed console logging for debugging

### **KMZ Export - Download Filename Fix** 🔧

**Issues Fixed:**
1. ✅ Downloaded KMZ files now have correct filename instead of generic "network_design.kmz"
2. ✅ CORS configuration updated to expose `Content-Disposition` header
3. ✅ Frontend enhanced to properly extract filename from response headers

**Technical Changes:**
- `backend/index.js`:
  - Added CORS `exposedHeaders` configuration for `Content-Disposition` and `Content-Type`
- `backend/routes.js`:
  - Explicitly set `Content-Disposition` header with RFC 5987 encoding
  - Added console logging for download filename verification
- `frontend/src/NetworkDesignTool.js`:
  - Enhanced filename extraction to handle multiple header formats
  - Added fallback patterns for RFC 5987 encoding and quoted/unquoted values
  - Added detailed console logging for header debugging

### **KMZ Export - Protected Service Option Fix** ✨

**Enhancement:**
- "Both Routes (Protected Service)" option now correctly disabled when secondary route is not available in design

**Technical Changes:**
- `frontend/src/NetworkDesignTool.js`:
  - Fixed "Both Routes" disabled logic: changed `&&` to `||` (line 3918)
  - Now properly disables protection export when only primary route exists

### **KMZ Export - Missing Files Confirmation** 🔔

**Enhancement:**
- Pre-export validation checks which circuits have missing KMZ files
- Displays confirmation dialog listing unavailable circuits with reasons
- User can choose to cancel or continue with partial export
- Prevents export if ALL circuits are missing KMZ files

**User Experience:**
1. User clicks "Export KMZ"
2. System checks availability of all required circuit KMZ files
3. If missing files detected:
   - Shows dialog with list of missing circuits
   - Displays circuit ID, route type (Primary/Secondary), and reason
   - User can cancel or continue with available circuits only
4. If all files available, export proceeds immediately

**Technical Changes:**
- **Backend** (`backend/routes.js`):
  - New endpoint: `POST /network_design/check_kmz_availability`
  - Validates circuit KMZ files in database and file system
  - Returns list of unavailable circuits with reasons
  - Checks both database `kmz_file_path` and physical file existence

- **Frontend** (`frontend/src/api.js`):
  - New API function: `checkKMZAvailability(data)`

- **Frontend** (`frontend/src/NetworkDesignTool.js`):
  - New state: `kmzMissingCircuitsDialogOpen`, `kmzMissingCircuits`, `kmzExportDataPending`
  - Split `handleKMZExport()` into two functions:
    - `handleKMZExport()` - Validates and checks availability (new)
    - `performKMZExport()` - Actually performs the export (refactored)
  - New handlers: `handleMissingCircuitsCancel()`, `handleMissingCircuitsContinue()`
  - New dialog component: "Missing Circuits Confirmation Dialog"
  - Added `WarningIcon` import from Material-UI

**Error Handling:**
- If all circuits missing: Shows error, prevents export
- If some circuits missing: Shows confirmation, allows partial export
- If none missing: Proceeds directly to export

### **KMZ Export - Default Selection** 🎯

**Enhancement:**
- Default selection in KMZ Export dialog changed from "Both Routes (Protected Service)" to "Primary Route Only"
- Provides better default behavior for most common use case

**Technical Changes:**
- `frontend/src/NetworkDesignTool.js`:
  - Changed initial state: `useState('primary')` (was `'both'`)
  - Changed reset value in `handleKMZExportClose()` to `'primary'` (was `'both'`)

### **🔴 CRITICAL: Protected Service Pricing Logic Overhaul** ⚠️

**BREAKING CHANGE - Protected Service Pricing Calculation:**

The protected service pricing logic has been completely rewritten to enforce margin targets and properly account for full redundancy costs.

**OLD Logic (Removed):**
- Primary path: 100%
- Secondary path: 70% (weighted by `protectionPathMultiplier`)
- Margins were informational, not enforced
- Formula: `Protected Price = Primary + (Secondary × 0.7)`

**NEW Logic (Enforced):**
- Primary path: 100%
- Secondary path: 100%
- **Margins are now ENFORCED** based on full allocated cost
- Formula: `Protected Allocated Cost = Primary Allocated + Secondary Allocated`
- Formula: `Protected Min Price = Allocated Cost / (1 - minMargin%)`
- Formula: `Protected Suggested Price = Allocated Cost / (1 - suggestedMargin%)`

**Key Changes:**

1. **Full Cost Recognition:**
   - Both primary and secondary allocated costs are included at 100%
   - Accurately reflects true cost of maintaining dual redundant paths

2. **Enforced Margin Targets:**
   - **12-month**: Min 50%, Suggested 70% (enforced)
   - **24-month**: Min 47.5%, Suggested 65% (enforced)
   - **36-month**: Min 45%, Suggested 60% (enforced)
   - Margins are calculated and guaranteed, not just informational

3. **Removed Configuration:**
   - `protectionPathMultiplier` (0.7) completely removed from:
     - Database (`pricing_logic_config` table)
     - Backend API (`GET/PUT /pricing_logic/config`)
     - Frontend UI (Pricing Logic Manager - "Additional Charges" section removed)
     - All default/seed configurations

4. **NRC Handling:**
   - Still charged once (from primary path only) - **NO CHANGE**

**Impact Example:**
```
Scenario: 12-month protected service
- Primary Allocated Cost: $600
- Secondary Allocated Cost: $800

OLD Calculation:
- Protected Allocated: $600 + ($800 × 0.7) = $1,160
- Protected Min (arbitrary): $2,000 (margin varies)

NEW Calculation:
- Protected Allocated: $600 + $800 = $1,400
- Protected Min (enforced 50%): $1,400 / 0.5 = $2,800 (exactly 50% margin)
- Protected Suggested (enforced 70%): $1,400 / 0.3 = $4,667 (exactly 70% margin)
```

**Why This Change:**
- ✅ Transparent, predictable pricing
- ✅ Margins always met (no guesswork)
- ✅ True cost of redundancy reflected
- ✅ Simplified configuration (less to maintain)

**Files Modified:**
- **Backend** (`backend/routes.js`):
  - Rewritten protected service pricing calculation (lines 5491-5598)
  - Removed `protectionPathMultiplier` from config endpoints
  - Updated default configurations
- **Frontend** (`frontend/src/PricingLogicManager.js`):
  - Removed "Additional Charges" card/UI section
  - Removed `protectionPathMultiplier` from state and reset functions
- **Frontend** (`frontend/src/AllocatedCostCalculator.js`):
  - Updated protected service description to reflect new 100%+100% logic (line 2460)
  - Removed outdated reference to 70% weighting

**Testing:**
- Created `backend/test_protected_pricing.js` test script
- Validates 5 scenarios across all contract terms
- Confirms 100%+100% cost calculation
- Confirms enforced margin targets
- Confirms old 70% logic is NOT used
- **All tests pass** ✅

**Migration Notes:**
- No database migration required
- Existing `charges.protectionPathMultiplier` entries in database are ignored/skipped
- Old pricing logic no longer accessible
- All new protected service quotes will use new calculation

### **Promo Pricing - Documentation Correction** 📝

**Issue:**
- Frontend UI text incorrectly stated "Promo pricing applies to primary routes only"
- Backend logic was correct - promo pricing applies to **BOTH** primary and secondary routes

**Fix:**
- Updated `PromoPricingManager.js` UI text (2 instances) to correctly state:
  - "Promo pricing is checked first and applies to both primary and secondary routes if margin requirements are met"
- Backend logic unchanged (was already correct)

**Technical Details:**
- Backend: Line 5253 comment correctly states "applies to both primary and secondary paths"
- Promo pricing checks `totalAllocatedCost` against margin requirements
- If margin is met, promo price is used for that path (primary OR secondary)
- This is the intended behavior and now correctly documented in UI

**Files Modified:**
- `frontend/src/PromoPricingManager.js` (lines 279, 457)

### **KMZ Viewer - Auto Zoom to London Metro** 🗺️

**Enhancement:**
- KMZ Viewer now automatically zooms into London metro area when opened
- Provides immediate context for UK-centric network routes
- Users can still use home button or manual navigation to view other regions

**Technical Details:**
- Initial camera position set to London coordinates: -0.1276°W, 51.5074°N
- Altitude: 500km (provides regional view of London and surrounding areas)
- Previous behavior: Started at global Earth view (required manual navigation)

**Files Modified:**
- `frontend/src/KMZMapViewer.js` (lines 107-109)

### **Live Latency - Staggered Bucket Refresh System** ⚡

**Performance Enhancement:**
- Completely redesigned automated live latency refresh to prevent "thundering herd" problem
- Previously: All 300+ circuits refreshed simultaneously every 15 minutes
- Now: Circuits divided into 4 buckets, refreshing 5 minutes apart

**New Architecture:**
- **Bucket System**: Circuits alphabetically sorted and split into 4 equal buckets
- **Refresh Frequency**: Each circuit still refreshes every **20 minutes** (5 minutes longer than before, but more reliable)
- **Staggered Timing**:
  - Bucket 0: Minutes 5, 25, 45... (~75 circuits)
  - Bucket 1: Minutes 10, 30, 50... (~75 circuits)
  - Bucket 2: Minutes 15, 35, 55... (~75 circuits)
  - Bucket 3: Minutes 20, 40, 60... (~75 circuits)
- **Startup**: Waits 5 minutes after server start before initiating staggered pattern

**Problem Solved:**
- ❌ **Before**: 300+ simultaneous API calls every 15 minutes
  - Overwhelmed external API endpoints
  - Application couldn't process all requests at once
  - Frequent failures and timeouts
- ✅ **After**: ~75 API calls every 5 minutes
  - Distributed load over time
  - API endpoints can handle smaller batches
  - Application processes each bucket smoothly
  - Reduced failure rate

**Automatic Scaling:**
- Bucket size automatically adjusts based on total circuits
- 300 circuits = 75 per bucket
- 400 circuits = 100 per bucket
- 500 circuits = 125 per bucket
- System remains stable up to ~800 circuits

**Technical Details:**
- Batch processing: Still 3 circuits at a time with 2-second delays (unchanged)
- Bucket assignment: Alphabetical sort ensures deterministic assignment
- Each bucket has independent interval timer (4 timers total)
- Latency warnings only updated after final bucket (Bucket 3) completes
- Processing time per bucket: ~50-90 seconds depending on circuit count

**Configuration:**
- Hardcoded: 4 buckets, 20-minute cycle, 5-minute offsets
- Suitable for up to 800 circuits without modification
- No database migration required

**Files Modified:**
- `backend/liveLatencyAutoRefreshService.js` (complete rewrite)

### **Network Routes - Bandwidth Filter Exact Match** 🔍

**Bug Fix:**
- Bandwidth filter now performs exact matching instead of substring matching
- Previously: Searching for "1000" would show routes with bandwidth 1000, 10000, 100000
- Now: Searching for "1000" only shows routes with exactly 1000 bandwidth

**Technical Details:**
- Changed from `.includes()` (substring match) to `===` (exact match)
- Ensures precise filtering when searching for specific bandwidth values
- Still handles "Dark Fiber" correctly as an exact string match

**Example:**
- Filter: "1000" → Shows only 1000 Mbps routes ✅
- Filter: "10000" → Shows only 10000 Mbps routes ✅
- Filter: "100000" → Shows only 100000 Mbps routes ✅
- Filter: "Dark Fiber" → Shows only Dark Fiber routes ✅

**Files Modified:**
- `frontend/src/App.js` (line 311)

### **User Management - Manage Exchange Rates Module Permission** 👥

**Enhancement:**
- Added "Manage Exchange Rates" to user module permissions
- Non-admin users can now be granted access to manage exchange rates
- Previously only administrators could access this module

**Permission Levels:**
- **Read-Only**: Can view exchange rates only
- **Provisioner**: Can view and edit exchange rates
- **Administrator**: Full access to all modules (unchanged)

**Use Case:**
- Finance or operations teams can be granted access to maintain exchange rates
- No need to grant full admin access just to manage currency exchange rates
- Follows same permission model as other modules

**Technical Details:**
- Module key: `exchange_rates`
- Module label: "Manage Exchange Rates"
- Backend already supported this module in `allModules` array
- Frontend now displays this option in user module permissions UI

**Files Modified:**
- `frontend/src/UserManagement.js` (line 53)

## 🐛 Known Issues

**None at this time**

---

## 🔄 Deployment Instructions

### **1. Stop Backend**
```bash
# Stop your current backend process
```

### **2. Install New Dependencies**
```bash
cd backend
npm install
```

### **3. Start Backend**
```bash
npm start
```

**Expected Console Output:**
```
🔄 Checking for database migrations...
▶️  Running migration: 014_add_kmz_templates.js
Running migration: Add kmz_templates table
✓ kmz_templates table created successfully
✓ Index on kmz_templates.template_type created
✓ Migration completed successfully
✓ All migrations completed successfully
```

### **4. Refresh Frontend**
- Hard refresh in browser: `Ctrl + Shift + R` (Windows) or `Cmd + Shift + R` (Mac)

### **5. Upload Templates (Admin)**
1. Login as administrator
2. Go to **Admin → System Settings**
3. Upload disclaimer.kmz
4. Upload locations.kmz (optional)

### **6. Test Export**
1. Go to Network Design & Pricing Tool
2. Create a design
3. Click "Export KMZ"
4. Verify download works

---

## 📦 Deployment & Maintenance

### **⚠️ Critical: Production Deployment Changes for v3.3.3**

Version 3.3.3 **REQUIRES** production builds and has specific deployment requirements:

#### **🚨 MUST READ Before Deploying:**
1. **NO development server** (`npm start`) - Will crash with memory errors
2. **MUST use production build** - `npm run build` + `serve`
3. **New files required** - `serve.json` configuration
4. **Updated PM2 config** - `ecosystem.config.js` must use `npx serve`

#### **📖 Complete Deployment Guides**

**For RHEL/Linux Production Servers:**
- 📘 **[RHEL_PRODUCTION_DEPLOYMENT_V3.3.3.md](RHEL_PRODUCTION_DEPLOYMENT_V3.3.3.md)** ← **RECOMMENDED**
  - Complete step-by-step guide
  - Includes all v3.3.3 specific requirements
  - Cesium setup instructions
  - Production build configuration
  - Troubleshooting for common issues

**For Quick Fixes:**
- 📙 **[PRODUCTION_DEPLOYMENT_FIX.md](PRODUCTION_DEPLOYMENT_FIX.md)** - Emergency fix for memory crashes
- 📙 **[ECOSYSTEM_CONFIG_CHANGES.md](ECOSYSTEM_CONFIG_CHANGES.md)** - PM2 configuration changes

**For Development:**
- 📙 **[DEPLOYMENT_SIZE_REDUCTION.md](DEPLOYMENT_SIZE_REDUCTION.md)** - Clean caches before deploying

#### **⚡ Quick Deployment Checklist**

Before deploying v3.3.3 to production:

```bash
# 1. Clean caches (dev machine)
./cleanup-deployment.sh

# 2. Build frontend (server)
cd frontend
export NODE_OPTIONS="--max-old-space-size=2048"
npm run build

# 3. Create serve.json (server)
cat > serve.json << 'EOF'
{
  "public": "build",
  "rewrites": [{"source": "**", "destination": "/index.html"}],
  "directoryListing": false
}
EOF

# 4. Install serve (server)
npm install -g serve

# 5. Update ecosystem.config.js frontend section:
# script: 'npx',
# args: ['serve', '-p', '3000', '--no-clipboard']

# 6. Restart PM2
pm2 restart ecosystem.config.js --env production
```

#### **Expected Space Savings from Cache Cleanup: 2.5-5GB**

---

---

## 🐛 **Bug Fixes (Post-Release)**

### **Issue 1: KMZ Export ENOENT Error (CRITICAL FIX)**
**Problem:** KMZ export failed with "ENOENT: no such file or directory" error  
**Cause:** `backend/temp/` directory was missing on production servers  
**Fix:** 
- Added automatic directory creation in `backend/index.js` on server startup
- Added safety check in `kmzGenerator.js` to create outputDir if missing
- Ensures `temp/`, `templates/`, `kmz_files/`, `logs/` directories always exist

**Files Changed:**
- `backend/index.js` - Added directory creation on startup
- `backend/kmzGenerator.js` - Added outputDir existence check

---

### **Issue 2: KMZ Export Progress Bar**
**Problem:** Users only saw a spinning wheel during KMZ export with no status indication  
**Enhancement:** Added step-based progress bar showing export stages  
**Features:**
- **5-stage progress tracking:**
  1. Validating circuits... (0-15%)
  2. Loading circuit KMZ files... (15-40%)
  3. Combining routes... (40-70%)
  4. Generating KMZ file... (70-90%)
  5. Export complete! (90-100%)
- Linear progress bar with percentage
- Step description text
- Displayed directly in export dialog

**Files Changed:**
- `frontend/src/NetworkDesignTool.js` - Added progress tracking states and UI

---

### **Issue 3: KMZ Download Naming from Network Routes Table**
**Problem:** Downloaded KMZ files had generic names like `kmz_file-timestamp.kmz`  
**Fix:** Downloads now use friendly format: `UCN - Source - Destination.kmz`  
**Example:** `LONLON123456 - IPCLON11 - IPCLON7.kmz`

**Technical Details:**
- **Backend:** Queries database for circuit info when serving download
- Filename constructed from `circuit_id`, `source_location_id`, `destination_location_id`
- Sets filename in `Content-Disposition` header with RFC 5987 encoding
- **Frontend:** Extracts filename from `Content-Disposition` header in response
- Supports multiple header formats: `filename*=UTF-8''...`, `filename="..."`, `filename=...`
- Falls back to original filename if header not present

**Files Changed:**
- `backend/routes.js` - Updated `/download_kmz/:filename` endpoint to query DB and set header
- `frontend/src/NetworkRoutesTable.js` - Updated `handleDownloadKMZ` to extract filename from header

---

### **Issue 4: KMZ Viewer Location Pins (CRITICAL FIX)**
**Problem:** 
- Location pins from circuit KMZ files appeared in wrong locations
- Duplicate pins from multiple circuit files
- Cluttered and inaccurate pin placement

**Solution:** 
- **Permanent location pins** loaded from `locations.kmz` template (uploaded in Admin → System Settings)
- **Circuit KMZ files** now only show route lines (polylines), NO pins/placemarks
- Locations template always visible, providing consistent accurate pin placement

**Technical Details:**
- Added permanent locations data source loaded on viewer initialization
- Modified circuit KMZ loading to filter out point-based entities:
  - Skips: `point`, `billboard`, `label` (without polyline)
  - Keeps: `polyline` (route lines only)
- Locations template loaded from `/kmz_templates/download/locations` endpoint
- Silently fails if template not yet uploaded (optional enhancement)
- **Pin visibility fix:** Ensures location pins remain visible at all zoom levels:
  - Set `distanceDisplayCondition` to `undefined` (no distance-based culling)
  - Set `disableDepthTestDistance` to `Number.POSITIVE_INFINITY` (always renders)
  - Set `scaleByDistance` to `undefined` (no auto-scaling)
  - Applied to `billboard`, `point`, and `label` entities

**Files Changed:**
- `frontend/src/KMZMapViewer.js` - Added locations template loading + entity filtering + zoom visibility fix

---

### **Issue 5: KMZ Viewer Route Color Customization**
**User Request:** "Is it possible so that users can change the colour of individual paths within KMZ viewer"

**Problem:** All routes displayed in the same red color, making it difficult to distinguish multiple routes visually.

**Solution:**
- Added per-route color customization with 8 preset colors
- Session-only (colors reset on page refresh, no database storage)
- Color picker button (palette icon) next to each route in sidebar
- Simple dropdown menu with color swatches

**Available Colors:**
- Red (default)
- Blue
- Green
- Yellow
- Orange
- Purple
- Pink
- Cyan

**How to Use:**
1. Load routes into KMZ Viewer
2. Click the palette icon (🎨) next to any route in the sidebar
3. Select a color from the dropdown menu
4. Route line color updates immediately on the globe

**Technical Details:**
- Color stored in component state (`routeColors` object)
- Session-only - resets when user refreshes page or closes viewer
- Updates all polyline entities in the route's DataSource
- Uses Cesium Color format (ABGR hex)
- Icon button shows current route color with colored border

**Files Changed:**
- `frontend/src/KMZMapViewer.js` - Added color picker UI, state management, and color change function

---

### **Issue 6: KMZ Viewer Performance Improvement**
**User Request:** "Initial load on the server is too long with dark fiber and 100Gb routes - change the default load to just Dark Fiber routes"

**Problem:** 
- KMZ Viewer auto-loaded both Dark Fiber (~50 routes) AND 100Gb routes on page load
- Combined load time was too slow (loading 50+ circuits simultaneously)

**Solution:**
- **Changed default filter:** Only Dark Fiber routes auto-load now (100Gb checkbox unchecked by default)
- Loads ~50 routes instead of 50 + 100Gb routes
- Routes still auto-load when filters change
- Users can manually enable 100Gb filter if needed

**Performance Impact:**
- **Before:** Loading Dark Fiber + 100Gb routes (~80-100+ routes)
- **After:** Loading only Dark Fiber routes (~50 routes)
- **Result:** ~40-50% reduction in initial load time

**Files Changed:**
- `frontend/src/KMZMapViewer.js` - Changed default `filters` state (gb_100: false)

---

## 📞 Support & Troubleshooting

### **🚨 "JavaScript heap out of memory" - Server Crashes**
**Symptoms:**
- PM2 logs show `FATAL ERROR: JavaScript heap out of memory`
- Frontend constantly restarting
- Memory usage ~900MB before crash

**Cause:** Running development server (`npm start`) instead of production build

**Fix:** See [PRODUCTION_DEPLOYMENT_FIX.md](PRODUCTION_DEPLOYMENT_FIX.md)

**Quick solution:**
```bash
cd /root/Core-Repository/frontend
npm run build
# Then update PM2 to use: serve -s build -l 3000
```

### **"Export button doesn't appear"**
- Ensure you have network_design module access
- Hard refresh browser
- Check console for errors

### **"Disclaimer template not found" error**
- Go to Admin → System Settings
- Upload disclaimer.kmz in KMZ Template Management section
- Template is required for all exports

### **"Some circuits were skipped"**
- Check that KMZ files exist for all circuits in the design
- KMZ files should be in `backend/kmz_files/` directory
- Filename format: `{circuit_id}.kmz` (e.g., `LONLON123456.kmz`)

### **"KMZ file won't open in Google Earth"**
- Verify file downloaded completely
- Check file extension is `.kmz`
- Try different KML viewer (Google Earth Pro, QGIS)
- Check backend logs for generation errors

### **Migration doesn't run**
- Ensure backend started successfully
- Check `migrations` table in database
- Review backend console output
- Delete migration entry from `migrations` table if needed to force re-run

---

## 🎉 Success!

Version 3.3.3 is now ready with full KMZ export capabilities! 🗺️

**Next Steps:**
1. Deploy to production
2. Train users on KMZ export feature
3. Upload company disclaimer template
4. Update user documentation

---

**For detailed version history, see:** `V3.3_RELEASE_NOTES.md`  
**For technical details, see:** `VERSION_HISTORY.md`

---

**Release prepared by:** AI Assistant  
**Release approved by:** [Your Name]  
**Deployment date:** [TBD]

