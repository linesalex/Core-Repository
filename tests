- In the network routes live latency table, ensure that if a circuit reports back 0ms as the live latency from the API - the colour of the field switches to red.  Currently it is green as long as the latency is not outside SLA.  This should be the case except for the latency being 0ms.  0ms should be in red.  
- The network routes live latency refresh should be automated every 15 mins.  Currently it is only on a manual refresh with a 15 minute cooldown.  Continue to allow the manual refresh button to work on a 15 min cooldown but also create an automated process to update the netowkr routes live latneyc table every 15 mins automatically. 
- In outage history, add date filters alongside filter for Circuit ID - Keep outage history for 
- Breach in expected latency warning tab to illistrate all services that breach expected latency number.  Include routes that breach expected latency by 5%.    
- Add ticket number reference field which is a manual addition into the current outages tab 
- in network routes when you click a live latency - the live latency inforamtion pop up occurs.  I want to remove the view history historical records.  This live latency history that shows daily snapshots can be removed.  Instead, i want this view history to show the latest API call logs view calculations - it should pop up and display the full 'Latency Calculation Details' of the most recent API Call within the specific circuit history.  
